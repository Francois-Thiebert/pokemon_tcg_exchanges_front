import { AdminGuardService } from './admin-guard-service';

describe('AdminGuardService', () => {
  it('should create an instance', () => {
    expect(new AdminGuardService()).toBeTruthy();
  });
});
